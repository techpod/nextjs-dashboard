import { Inter, Lusitana, Roboto, Orbitron, Dancing_Script } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const roboto = Roboto({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const orbitron = Orbitron({
  weight: ['400', '800'],
  subsets: ['latin'],
});

export const dancing_script = Dancing_Script({
  weight: ['400', '600'],
  subsets: ['latin'],
});